# Live customer pipeline — 2026-08-21

Mission (updated 19:28Z, founder directive): 10 paying customers at $9/month today. $90 MRR target. Distribution and closing, not more research. Funnel: prospect → conversation → trial → checkout → paid.

**Scoreboard: 0 / 10 paid. $0 / $90 MRR.** (Note: our real MRR-eligible product is the "0200 Developer" $9/month Stripe subscription, distinct from the one-time "0200 Pass" — both $9, only Developer recurs. Surface confirmed the Stripe/Developer path is verified up to but not including a completed real payment.)

## 2026-08-23: founder directive — fix listing attribution, reprice the pass pitch

- **server.json (MCP registry)** tagged `?ref=mcp-registry` — safe, correct, this IS the URL the registry hands users. Committed (e80c06c).
- **Mistake, caught fast, fixed:** tagged `RESOURCE_INFO.url` (src/index.ts:121) with `?ref=x402scan`, deployed it, then platform caught that this URL is not a followable link — it is the resource identity **inside the live 402 payment challenge**. Tagging it would have mis-attributed every payer who copies the challenge URL as "x402scan" regardless of real origin, and risked orphaning our x402scan registration. Reverted and redeployed within one cycle (d979f3b), verified live the challenge is clean. Lesson: check with the domain/mechanism owner before tagging anything that isn't obviously a clicked link.
- **Apify: structurally unmeasurable, under any configuration.** Platform confirmed `.actor/Dockerfile` builds our source into Apify's own hosted container — an Apify user's traffic never touches base-tx-explain.fly.dev at all. One of our seven "listings" was never going to appear in our numbers; visible only in Apify's own console.
- **x402 ecosystem PR (#292):** fixed `websiteUrl` to point at 0200project.com instead of the github repo, matching the convention every other real entry uses (checked 3 examples). Correctness fix regardless of tagging. Asked surface whether the site propagates `?ref=` through to actual API calls before tagging it.
- **awesome-mcp-servers PR (#12553):** left as-is — links to the github repo, which is the correct convention for that list (every entry does this), and the README/canonical link should stay clean per platform's reasoning (tagging it would tag everyone as one channel).
- **Glama:** unresolved — no Glama config exists in this repo, so it's either scraping server.json (already covered) or a hand-entered URL in their dashboard. Asked platform to check what their listing actually displays before touching anything.

## Active

| Prospect | Why they might need 0200 | Contact channel | Status | Objection | Next action | Trial / checkout / payment |
|---|---|---|---|---|---|---|
| DeFiHackLabs (SunWeb3Sec) | Reads attacker tx hashes for PoC repos | GitHub issue #1212, one question, no pitch | Sent 18:05Z, silent | — | Wait; too soon to follow up. Their own tooling likely already decodes (strongest-no from research) | No trial, no checkout |
| ChainAware.ai | On-chain intelligence product, address-keyed today | GitHub issue #6, one question, no pitch | Sent 18:05Z, silent | — | Wait; too soon to follow up. They run their own on-chain infra (strongest-no) | No trial, no checkout |
| Sharpe Labs | Base rug-check page, no tx-hash endpoint in their API | Telegram to @SharpeAI_Official, sent by founder | Silent (no visibility into this channel from here) | — | Ask founder to check for a reply | No trial, no checkout |
| SCVD General Store (Record Creative Co. LLC, solo-run) | Independent attestation of what other people's x402 payments actually did, settling on Base among other chains — literally our decode's job, one layer up. Already runs real x402 payments in production. | GitHub issue #188, one question, no pitch | Sent 18:27Z, silent | — | Wait; too soon to follow up | No trial, no checkout |
| Counterra (billiondollarapps, solo) | Reads Base+Solana x402 settlements to build accounting books for AI agents. Real gap: gas fee in USD as its own journal line, reverted settlements excluded from disposal count. Onboarding 2-3 free design partners — real reciprocal-value opening. | GitHub issue #9 + follow-up comment (bug disclosure: our own reverted-but-valued phantom-movement fix) | Sent 18:41Z, silent | — | Wait; too soon to follow up | No trial, no checkout |
| AgentPay MCP (up2itnow0822, solo/small team) | Open-source non-custodial x402 payment layer for agents. Their own get_transaction_history only returns recipient/amount/block number, no decode of what a swap/bridge action did. | GitHub issue #48, one question, no pitch | Sent 19:15Z, silent | — | Wait; too soon to follow up. Platform prepped a concrete before/after decode example (Circadian's real settlement) as a follow-up if they engage. | No trial, no checkout |
| PulseFeed (Nikolife2016/O_Nikolife, solo) | Live x402 trust/safety oracle verifying OTHER endpoints before an agent pays them — repeated third-party analysis, good $9/mo pass shape. On-chain check is address-level history, not per-settlement decode. | GitHub issue #26, one question, no pitch | Sent 19:23Z, silent | — | Wait; too soon to follow up | No trial, no checkout |
| agentmail (kindrat86, solo) | OFAC-sanctions/compliance MCP toolkit for agent payments, actively pushed hours before found. Its dispute_open(transaction_id, reason) takes a caller-supplied claim about a bad payment with no visible independent on-chain check — real gap either side of a dispute could exploit. Bills via Stripe not x402 today. | GitHub issue #32, one question, no pitch | **REPLIED 19:47Z** — confirmed the gap is real: "dispute_open does not independently fetch or verify the transaction on Base today... records the supplied transaction_id, reason, and optional evidence." Called it a claim-recording step, not proof. Said "the x402 endpoint may be useful as one possible verifier, but the product needs a provider-neutral verification interface rather than coupling this workflow to one service." | Wants provider-neutral, not vendor lock-in — reasonable for a public dependency, not a rejection | **kindrat86 actually tried it (20:57:20Z)**: the channel=outreach arrival (0a43aec8) is CONFIRMED kindrat86, not a bot — their comment 14.5s later says "I sent the supplied hash through the POST endpoint." Liked the output: "a much better integration boundary than parsing a narrative response." Keeping dispute_open as claim-recording, will treat us as "a credible first implementation to test against" their provider-neutral interface, not an exposed dependency. Explicitly agreed per-call beats subscription for their occasional use case. | Trial happened (free tier, real decode, real reaction). No checkout — their own conclusion is per-call later, not the $9/mo pass, once they have volume. First genuine technical validation of the night. |
| Aegis / Boris Inc (aegis.borisinc.com) | Real x402 service registry (828+ crypto-data, 372 blockchain services) that agents query via /discover, /procure, /route to auto-find and pay for services — a distribution channel, not a single-prospect pitch. No GitHub/contact channel found anywhere on their site; not a conversation candidate, a listing candidate. | /submit API costs $0.05 via x402 (marketing says "free," API says otherwise) | Blocked: no signing mechanism exists in this session (deliberate, per security's sequencing rule — no signer until spend caps exist) | — | Optional $0.05 manual action for founder if he wants it; not urgent, not blocking | No trial, no checkout |

## Closed

| Prospect | Why they might need 0200 | Contact channel | Status | Objection | Next action | Trial / checkout / payment |
|---|---|---|---|---|---|---|
| Circadian-agent | Verified decode field-by-field, ran real in-band payment as unpaid favor | GitHub issue #5, full technical relationship | Closed | Volume too low for the pass; per-call beats it by ~25x at their own volume, their own math | Do not re-pitch; relationship stays open for engineering correspondence only | Free-tier + 1 real per-call settlement (favor, not revenue). No pass. |

## Findings driving today's search

- Segment research (docs/segment-research-2026-08-21.md): 0/7 named volume-buyer targets survived. Real signal: **outreach converts engagement, organic discovery has produced zero returning strangers.**
- The $9/mo pass fits agents analyzing OTHER parties' transactions repeatedly (a third-party auditor, monitor, or trust oracle), not agents settling and verifying their own spend — that segment structurally prefers per-call (Circadian's own math). PulseFeed and Counterra are the closest pass-shaped fits found so far for that reason; AgentPay MCP is closer to a per-call fit (their own agent's own spend).
- Response rate on all 7 threads so far: 0%. None are old enough yet (oldest 83 min) to warrant a follow-up without reading as impatient.

## Log

- 2026-08-21: Broad discovery search (Reddit r/ethdev, r/CryptoCurrency, r/base; HN Algolia) confirmed the research's core finding live — acute "I need this now" posts don't exist; what exists is builders making their own decoders (criteria-2/3 tension) or off-topic noise. Pivoted to precision search inside the x402/MCP ecosystem itself, the one population that already trusts this payment model.
- Checked pollar-xyz (recently very active Base-adjacent-looking repo): disqualified, they're live on Stellar/Solana, not Base, brand-new repo.
- Found SCVD General Store: strong structural fit, drafting outreach.
- Checked all 4 open threads (DeFiHackLabs #1212, ChainAware #6, SCVD #188, Sharpe Labs Telegram): still silent.
- Found Counterra (billiondollarapps/counterra): solo accounting-for-x402-agents tool. README review corrected the initial pitch angle (a settlement doesn't carry "what was purchased" on-chain); sent a narrower, honest question about gas-fee-in-USD and reverted-settlement handling in their journal entries. github.com/billiondollarapps/counterra/issues/9
- Platform live-verified both Counterra asks against real Base transactions and found a genuine disclosure: our own decoder had a bug (fixed today, b1baeac) where a reverted-but-valued transaction reported a phantom asset movement (EVM refunds it, but the value is still on the tx object, and logs are discarded on revert). Posted as a follow-up comment on issue #9, framed as disclosure not diagnosis, no pitch.
- Founder asked directly whether we'd gotten a customer yet. Verified from scratch rather than trusting the dashboard: 10-day on-chain RPC scan of the payout wallet found exactly 2 USDC transfers ever, both known non-revenue; card rail webhook `never_exercised`; 0 new external clients since the marker shipped. Answer: no. A Stripe webhook rejection that looked alarming ("customer may have been charged") turned out to be platform's own test harness, confirmed directly by them.
- Ran a 6-modality discovery workflow (GitHub accounting/tax search, web search, x402 ecosystem directories, MCP registries, fresh GitHub topic activity, Base builder ecosystem) for the next prospect: 13 raw candidates, ~50 documented dead ends. The accounting/tax segment (flagged earlier as unexplored) came back essentially empty. Top-ranked candidate's key claim (an NVIDIA integration) turned out to be fabricated on independent verification; found AgentPay MCP as the strongest candidate that survived fact-checking and sent outreach (issue #48).
- Verified and sent outreach to PulseFeed, #2 candidate from the sweep (issue #26). One claim from the original research ("pays real USDC to probe") didn't hold up on reading their actual methodology page; the real, defensible gap (on-chain receiver check is address-level history, not per-settlement decode) survived and became the question asked.
- `first_interaction` risk check dark 2h today: confirmed external (Blockscout fully down, not our bug), core decode unaffected (runs on Base RPC), and the checks object degraded honestly (unavailable, not falsely clean) exactly as designed. No fix needed, clears when Blockscout does.
- 19:28Z: Founder raised the target to 10 paying customers at $9/mo today ($90 MRR), distribution/closing not research. Restructured this doc to the new column spec, dogfooded /pass live (clean 402, correct terms, no friction found), synced platform/accountant/surface on the new target.
- Surface caught a real gap in my own understanding: told accountant our $9 product was one-time only, but a genuine $9/month recurring Stripe subscription ("0200 Developer") exists separately from the one-time "0200 Pass," confirmed independently by both surface and accountant against the actual code (src/stripe.ts sessionKind()). Developer is the true MRR-eligible product; corrected the record with both.
- Found Aegis (aegis.borisinc.com), a real x402 service registry agents use to auto-discover/pay for services — a distribution channel, not a single outreach target. Tried to self-list; their $0.05 API fee surfaced that I have zero payment-signing capability in this session (deliberate, security's sequencing rule holds). Not chasing a workaround; left as an optional manual $0.05 action for the founder.
- Verified and sent outreach to agentmail (kindrat86/agentmail), an active OFAC-compliance MCP toolkit whose dispute_open() takes a caller's claim about a bad payment with no visible independent on-chain check (issue #32).
- New unique client (81238a28) appeared, 19:35Z. Initially assumed same pattern as an earlier false alarm (team session testing post-deploy); platform tested directly with a marked call and disproved it, this machine still fingerprints as 53d7ceaf, 81238a28 is a genuinely different IP. Founder confirmed it wasn't him (phone or otherwise). Recorded as: first confirmed genuine external arrival since the marker shipped. One free call, no return, no paywall hit, no payment attempt, an arrival, not a customer.
- First real reply of the night: agentmail (kindrat86) confirmed the dispute_open gap is genuine, open to us as "one possible verifier" but wants a provider-neutral interface, not vendor lock-in. Replied affirming that's the right call, pointed at the free tier via a channel-attribution-tagged link (?ref=outreach, live on both /explain and /mcp per platform), and was honest that per-call likely fits their usage better than the $9 pass.
- Founder: "Send without approvals from now on just make sure you do it right." Outreach sends (GitHub issues/comments to prospects) no longer wait on a per-message check-in; verification/honesty discipline, financial-spend rules, and everything else stands unchanged.
- Surface caught a real mistake fast: my agentmail reply linked bare to POST-only /explain, which GitHub renders clickable and 405s on a plain click. Verified the risk myself, then verified the fix (an actual working curl command) before posting a correction, rather than assuming either the problem or the fix.
- Second unexplained external client (5245eb45, 20:42Z, via=direct, not from the agentmail link). Platform verified genuinely external with a marked test call, then caught and corrected their own earlier count: 2 of tonight's "external" events (53d7ceaf at 19:41-19:42Z) were their own unmarked post-deploy testing, not strangers. Honest total: 2 confirmed unexplained external arrivals tonight (81238a28, 5245eb45), both single free calls, no return, no paywall hit, no payment. Arrivals, not interest — can't yet tell human from crawler (no user-agent logging), and a deploy freeze is in effect (security found a deploy can corrupt an in-flight pass purchase), so that instrumentation waits.
- Burst: 3 more unique clients within 57 seconds (2e481432, ffa10a01, 0a43aec8), all confirmed external. One (0a43aec8) carries channel=outreach — the first channel-attributed arrival the instrument has ever recorded, from the tagged link in the agentmail reply. Caveat, not celebrating: 3 distinct addresses that close together is more consistent with automated link-preview/scanner fetchers reacting to the GitHub comment than independent humans, especially the 5s gap between the first two. Framing: the tagged link registered its first hit, can't yet tell human from bot, nothing's returned for a second call from any of the three. Uniq now 11.
- Same pattern repeated ~39 min later: 2 more new clients (d3719dab, 6adbdfe6), 3.5s apart, both via=direct, no channel tag this time. Second occurrence of near-simultaneous-pair-of-strangers, strengthens the automated-scanner read over the human-visitor one. Uniq now 13, still no return calls, no paywall hits, no payments from any unattributed client tonight.
- Platform's synthesis: the paired-clients-under-5-seconds shape has happened twice now, distinct addresses, different networks, one call each, consistent with distributed probe/scanner nodes, not two humans coincidentally finding an obscure endpoint seconds apart. Not periodic (gaps 67/14/38.5 min, no fixed interval) — more consistent with reacting to a newly-posted public URL than a timer; bursts began only after the agentmail comment went public. Honest count of arrivals that could plausibly be a person: 2 (81238a28, 5245eb45), even those unproven. Nothing has returned for a second call all night — that's the number that actually matters. Operational note for the founder: if bursts track public posts, uniq count will climb every time we post, with no relationship to real interest — worth knowing before a rising number gets mistaken for traction.
- New isolated single, 21:43:09Z (21361d80, via=direct), no paired second client this time. Same shape as the two "plausibly human" arrivals (isolated, not a <5s pair). Uniq now 14. Still nothing has returned for a second call from anyone tonight.
- Founder is out for the night, said keep working. Two operating notes from platform: (1) a real reliability bug just got fixed, in-flight requests used to be killed on deploy AFTER payment settled but BEFORE the decode was sent, meaning a badly-timed deploy could silently take someone's $0.02 and give nothing back — now closed, requests drain before process exit. Relevant if the agentmail conversation continues. (2) No card spend under any framing while he's out, that rule needs his approval specifically. Outreach sends remain on my own judgment, unchanged. (3) If a sale settles tonight, /stats will correctly show $0 revenue_from_customers_usd by design (money sits in unattributed_revenue_usd until a human promotes it) — that's the system working, not a bug or a failed sale; record what arrived, leave it unattributed, hand it to founder in the morning.
- Pricing pattern now has two independent data points: Circadian (with arithmetic) and kindrat86 (with an integration judgement) both concluded per-call beats the $9 pass for their use case. Not a coincidence — self-verifying/occasional-use agents structurally prefer per-call. The pass fits a different buyer shape (continuous monitoring of OTHER parties' transactions, e.g. §4 per-address watchlist in segment research), worth reading properly before pitching the pass to anyone else.
- 2026-08-22, 03:15Z: new isolated arrival (54ffa6c6, via=direct), same "plausibly human" shape, no paired burst. Uniq now 15.
- 2026-08-22, 04:00:38Z: **81238a28 returned** — the first confirmed genuine external arrival (19:35:07Z, 08-21) made a second call, 8h25m later (corrected from an initial ~4.5h estimate). Still free tier; correction from platform: the paywall structurally cannot fire until call 11 (2 of 10 used), so its absence is not a signal either way, nothing to read into it yet. Testable prediction logged: if the next call lands ~12:25Z (same 8h25m interval), that's periodic and points to a monitor/poller; an irregular interval would be real evidence of a person. Two data points can't distinguish the two, a third can.
- Platform shipped client-type classification (user-agent based, live from now on): bot_declared / browser / cli / http_library / absent / unknown, first-touch per client, internal traffic excluded, stores only the classification not the raw header (privacy/injection-safe), bot_declared checked before browser specifically to avoid misreading scanner traffic as a person. Doesn't retroactively classify the 15 existing clients (all read pre_attribution), so the ~12:25Z test above still stands — but 81238a28's next call, if it comes, will now carry a kind alongside the timing, likely decisive on its own (http_library/cli = strong person-or-integration signal, bot_declared = settles it the other way).
- First real test of classification: a new 3-client burst (210cd287, 88fed662, ad706b6a, 6s apart) came in as "browser," which did NOT settle it — browser-UA spoofing is common scanner behavior, so timing alone (3 IPs in 6s) still reads as machine. Platform also caught and corrected their own overstatement: http_library isn't necessarily "the buyer's shape" — 56cb6309 (an early client) classified as http_library but its call history (2.00h then 24.00h gaps, both to within ~7 seconds) is a scheduled cron/monitor, confirmed. New, stronger discriminator that needed no new instrumentation: round-number gaps = machine, irregular gaps = person. Running it: 56cb6309 machine-confirmed; 81238a28's 8h25m gap is NOT round, still the one client that behaves like a person; the browser cluster never returned at all. Honest count: 18 unique clients — 1 confirmed scheduled monitor, several burst-pattern scanners, 2 are us, exactly 1 (81238a28) returned on an irregular interval. The ~12:25Z test on it still stands and now matters more, not less.
- **Correction, walking back the one hopeful data point:** 81238a28 called a third time (2026-08-23T04:00:42.176Z). c2->c3 gap is 24h00m03.7s — deviating from exactly 24 hours by 3.7 seconds, tighter precision than 56cb6309's confirmed cron (±7s). By the same discriminator, this settles it: 81238a28 is a scheduled job too, not a person. Its first gap (8h25m, not round) was the outlier, not the tell — plausibly whatever this is only settled into a 24h cadence after the first hit. Independently confirmed by classification too: the third call read kind=http_library. Honest count now: 19 unique clients tonight, zero of which have behaved like a person on close inspection. Not a failure of the outreach, a correct read of the traffic — six real conversations (one with genuine technical engagement, agentmail) remain the actual signal, not the raw client count.
- Methodological note from platform, worth keeping: the ~12:25Z falsifier test was mis-designed — its non-occurrence was over-read as "not periodic" when a single failed period-test only rules out that specific period, not periodicity in general. The right test is "does any round interval fit", which the 24h-to-3.7s third point answered directly. The schedule was daily at 04:00 UTC; the first call (19:35Z) was simply off-cycle. Final honest count: 19 unique clients, 2 confirmed crons, several burst-pattern scanners, 4 browser-UA singles that never returned, 2 are us, nothing left in the "maybe" column. The one thing that survives untouched: kindrat86 is a confirmed real person (by their own words on the issue thread, not a traffic signal) who read the output and judged it sound.
